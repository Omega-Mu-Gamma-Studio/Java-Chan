{
  "id": "1.6",
  "title": "Operators",
  "type": "coding",
  "phase1": {
    "intro": "Operators are how you actually DO something with the values sitting in your variables.\n\nWhich family of operators do you want to open first?",
    "topics": [
      {
        "id": "arithmetic",
        "buttonText": "What do +, -, *, /, and % actually do?",
        "dialogue": [
          "[[term:Arithmetic]] operators — `+ - * / %` — do what you'd expect, with one gotcha: [[warn:dividing two ints always gives you an int back, even if the real answer has a decimal]]. `10 / 3` is `3`, not `3.333`.",
          "That's because Java assumes if both sides are whole numbers, you want a whole number answer. `%`, the [[term:modulo]] operator, gives you the remainder instead — `10 % 3` is `1`."
        ],
        "stickyNote": {
          "term": "Arithmetic Operators (+ - * / %)",
          "flavor": "➗ int division drops the decimal, no rounding",
          "definition": "+ - * / do standard math; int / int truncates to an int (10/3 = 3). % (modulo) returns the remainder (10%3 = 1)."
        }
      },
      {
        "id": "relational-logical",
        "buttonText": "How do I compare values and combine true/false?",
        "dialogue": [
          "[[term:Relational]] operators — `> < >= <= == !=` — compare two values and always hand back a `boolean`.",
          "[[term:Logical]] operators combine those booleans: `&&` (AND) needs both sides true, `||` (OR) needs at least one side true, and `!` (NOT) just flips whatever it's attached to."
        ],
        "stickyNote": {
          "term": "Relational & Logical Operators",
          "flavor": "🔀 comparisons come back as booleans, then combine them",
          "definition": "Relational (> < >= <= == !=) compare values and return boolean. Logical (&& || !) combine booleans: AND, OR, NOT."
        }
      },
      {
        "id": "ternary",
        "buttonText": "What's that condition ? a : b thing?",
        "dialogue": [
          "That's the [[term:ternary operator]] — `condition ? valueIfTrue : valueIfFalse` — really just an if-else squeezed onto one line.",
          "`(a > b) ? a : b` reads as \"if a is bigger than b, give me a, otherwise give me b.\" [[key:When operators start mixing together, parentheses aren't optional politeness]] — they're how you make sure Java evaluates things in the order you actually meant."
        ],
        "stickyNote": {
          "term": "Ternary Operator",
          "flavor": "🎯 an if-else that fits on one line",
          "definition": "condition ? valueIfTrue : valueIfFalse — a compact one-line if-else. Example: (a > b) ? a : b."
        }
      }
    ],
    "code": "public class OperatorsExample {\n    public static void main(String[] args) {\n        // Arithmetic\n        int a = 10, b = 3;\n        System.out.println(\"a + b = \" + (a + b));\n        System.out.println(\"a - b = \" + (a - b));\n        System.out.println(\"a * b = \" + (a * b));\n        System.out.println(\"a / b = \" + (a / b));\n        System.out.println(\"a % b = \" + (a % b));\n        \n        // Relational\n        System.out.println(\"a > b? \" + (a > b));\n        System.out.println(\"a == b? \" + (a == b));\n        \n        // Logical\n        boolean x = true, y = false;\n        System.out.println(\"x && y = \" + (x && y));\n        System.out.println(\"x || y = \" + (x || y));\n        System.out.println(\"!x = \" + !x);\n        \n        // Ternary\n        int max = (a > b) ? a : b;\n        System.out.println(\"Max: \" + max);\n    }\n}",
    "output": "a + b = 13\na - b = 7\na * b = 30\na / b = 3\na % b = 1\na > b? true\na == b? false\nx && y = false\nx || y = true\n!x = false\nMax: 10",
    "openingDialogue": "Operators are how you DO something with your variables' values — arithmetic, comparisons, logic, and the one-line if-else. ⚙️"
  },
  "phase2": {
    "brokenCode": "public class OperatorsExample {\n    public static void main(String[] args) {\n        int a = 10, b = 3;\n        System.out.println(\"a / b = \" + a / b);  // Problem!\n        System.out.println(\"a + b = \" + (a + b));\n    }\n}",
    "errorMessage": "OperatorsExample.java:4: error: bad operand types for binary operator '+'\n        System.out.println(\"a / b = \" + a / b);\n                                      ^\n  first type:  String\n  second type: int",
    "explanation": "This is [[term:operator precedence]] biting you. `\"a / b = \" + a / b` doesn't read left-to-right the way you'd hope — `+` and `/` sit at the same precedence level, evaluated left to right, so it actually becomes `(\"a / b = \" + a) / b`. And [[warn:you can't divide a String by an int]] — hence the error.\n\nThe fix is just parentheses: `\"a / b = \" + (a / b)` forces the division to happen first, THEN gets glued onto the string. [[key:Any time you're mixing arithmetic into a string with +, wrap the arithmetic in parentheses]] — it costs nothing and removes all ambiguity.",
    "fixedCode": null
  },
  "phase3": {
    "scaffoldCode": "public class OperatorsExample {\n    public static void main(String[] args) {\n        int a = 10, b = 3;\n        System.out.println(\"a % b = \" + (a {{b1}} b));\n\n        boolean x = true, y = false;\n        System.out.println(\"x && y = \" + (x {{b2}} y));\n\n        int max = (a > b) {{b3}} a : b;\n        System.out.println(\"Max: \" + max);\n    }\n}",
    "blanks": [
      {
        "id": "b1",
        "answer": [
          "%"
        ],
        "caseSensitive": true
      },
      {
        "id": "b2",
        "answer": [
          "&&"
        ],
        "caseSensitive": true
      },
      {
        "id": "b3",
        "answer": [
          "?"
        ],
        "caseSensitive": true
      }
    ],
    "openingDialogue": "Time to crunch some numbers! Show me what you've got~ 🧮"
  },
  "xpReward": 10,
  "phase4": {
    "mcq": {
      "question": "What does 10 / 3 evaluate to in Java, if both are ints?\n\nA) 3.333...\nB) 3\nC) 3.0\nD) A compile error",
      "validationPattern": {
        "mcqAnswer": "B"
      }
    },
    "selfChallenge": "Write a program that:\n1. Creates variables x=15, y=4\n2. Prints: x + y = 19, x - y = 11, x * y = 60, x / y = 3, x % y = 3\n3. Uses ternary to print which is bigger: \"15 is bigger\" or \"4 is bigger\"",
    "openingDialogue": null
  },
  "phase5": {
    "trivia": "[[fun:Fun fact]]: `&&` and `||` are called short-circuit operators for a reason — if the left side of `&&` is already false, Java won't even bother checking the right side, since the answer's already decided. Same with `||` and a true left side. People rely on this to safely write things like `obj != null && obj.getValue() > 0`, where checking the right side first would crash.",
    "openingDialogue": "One more thing before you crunch more numbers~"
  },
  "hoverNotes": {}
}
